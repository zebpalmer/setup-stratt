[bump]
current_version = "0.2.0"

[[bump.files]]
filename = "stratt.toml"
search  = 'current_version = "{current_version}"'
replace = 'current_version = "{new_version}"'

[tasks.lint]
description = "Validate action YAML with actionlint"
run = "actionlint"
